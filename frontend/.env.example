# Copy to .env
#
# Development (recommended): leave empty and run `python api.py` on port 5000.
# Vite proxies /api → http://localhost:5000
VITE_API_BASE=
#
# Production or direct calls without proxy:
# VITE_API_BASE=http://localhost:5000
